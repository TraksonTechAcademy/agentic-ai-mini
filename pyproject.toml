[project]
name = "agentic-ai-mini"
version = "0.1.0"
description = "A minimal agent loop with tool use + evaluation harness (LLM-agnostic)."
requires-python = ">=3.10"
authors = [{name="Ragouguelaba Agoda Koussema"}]

[tool.ruff]
line-length = 100
target-version = "py310"
